# Defense-in-depth: ignore everything except this gitignore and the example file.
# user-data/ is already gitignored at the repo root, but this guards against
# anyone accidentally un-ignoring it.
*
!.gitignore
!.env.example
!README.md
