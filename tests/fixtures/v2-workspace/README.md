# v2 Workspace Fixture

Synthetic v2.1.0 workspace used by `tests/migrate-v3.test.js`. The file shapes mirror real v2 workspaces but content is minimal and deterministic.

When updating v2-related tests, update this fixture to match.
